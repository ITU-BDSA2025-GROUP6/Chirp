name: Move assigned issues to the To Do column in the Project board
on:
  issues: 
    types: [assigned]
    
jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps: 
      - uses: alex-page/github-project-automation-plus@v0.8.3
        with:
          project: Backlog
          column: Todo